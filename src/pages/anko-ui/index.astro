---
import Layout from '../../layouts/Layout.astro';
import { css } from '../../styled-system/css';
import { button } from '../../styled-system/recipes';
import { token } from '../../styled-system/tokens';
import '../../styled-system/styles.css';
---

<Layout title="Anko-UI - Modern Design System">
  <div class={css({
    minHeight: '100vh',
    background: 'linear-gradient(135deg, teal.50 0%, mint.50 100%)'
  })}>
    <!-- Hero Section -->
    <section class={css({
      padding: '4rem 0',
      textAlign: 'center'
    })}>
      <div class={css({
        maxWidth: '1200px',
        margin: '0 auto',
        padding: '0 2rem'
      })}>
        <h1 class={css({
          fontSize: '4rem',
          fontWeight: 'bold',
          marginBottom: '1rem',
          background: 'linear-gradient(135deg, teal.500, mint.400)',
          backgroundClip: 'text',
          color: 'transparent',
          '@media (max-width: 768px)': {
            fontSize: '2.5rem'
          }
        })}>
          Anko-UI
        </h1>
        <p class={css({
          fontSize: '1.5rem',
          color: 'gray.600',
          marginBottom: '2rem',
          maxWidth: '600px',
          margin: '0 auto 2rem'
        })}>
          美しく、機能的で、型安全なデザインシステム<br>
          Panda CSS × TypeScript で構築
        </p>
        <div class={css({
          display: 'flex',
          gap: '1rem',
          justifyContent: 'center',
          '@media (max-width: 768px)': {
            flexDirection: 'column',
            alignItems: 'center'
          }
        })}>
          <a href="/anko-ui/docs" class={button({ variant: 'primary', size: 'lg' })}>
            ドキュメント
          </a>
          <a href="/anko-ui/playground" class={button({ variant: 'outline', size: 'lg' })}>
            プレイグラウンド
          </a>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class={css({
      padding: '4rem 0',
      background: 'white'
    })}>
      <div class={css({
        maxWidth: '1200px',
        margin: '0 auto',
        padding: '0 2rem'
      })}>
        <h2 class={css({
          fontSize: '2.5rem',
          fontWeight: 'bold',
          textAlign: 'center',
          marginBottom: '3rem',
          color: 'gray.900'
        })}>
          特徴
        </h2>
        <div class={css({
          display: 'grid',
          gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
          gap: '2rem'
        })}>
          <!-- Feature 1 -->
          <div class={css({
            padding: '2rem',
            border: '1px solid',
            borderColor: 'gray.200',
            borderRadius: '12px',
            textAlign: 'center',
            background: 'teal.50'
          })}>
            <div class={css({
              width: '60px',
              height: '60px',
              background: 'teal.100',
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              margin: '0 auto 1rem',
              fontSize: '1.5rem'
            })}>
              🎨
            </div>
            <h3 class={css({
              fontSize: '1.25rem',
              fontWeight: 'bold',
              marginBottom: '1rem',
              color: 'gray.900'
            })}>
              美しいデザイン
            </h3>
            <p class={css({
              color: 'gray.600',
              lineHeight: '1.6'
            })}>
              洗練されたカラーパレットと構造化されたトークンシステムで、一貫性のある美しいUIを構築
            </p>
          </div>

          <!-- Feature 2 -->
          <div class={css({
            padding: '2rem',
            border: '1px solid',
            borderColor: 'gray.200',
            borderRadius: '12px',
            textAlign: 'center',
            background: 'mint.50'
          })}>
            <div class={css({
              width: '60px',
              height: '60px',
              background: 'mint.100',
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              margin: '0 auto 1rem',
              fontSize: '1.5rem'
            })}>
              ⚡
            </div>
            <h3 class={css({
              fontSize: '1.25rem',
              fontWeight: 'bold',
              marginBottom: '1rem',
              color: 'gray.900'
            })}>
              高速・軽量
            </h3>
            <p class={css({
              color: 'gray.600',
              lineHeight: '1.6'
            })}>
              Panda CSSの力で、使用したスタイルのみをビルド時に生成。最適化されたCSSを出力
            </p>
          </div>

          <!-- Feature 3 -->
          <div class={css({
            padding: '2rem',
            border: '1px solid',
            borderColor: 'gray.200',
            borderRadius: '12px',
            textAlign: 'center',
            background: 'sage.50'
          })}>
            <div class={css({
              width: '60px',
              height: '60px',
              background: 'sage.100',
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              margin: '0 auto 1rem',
              fontSize: '1.5rem'
            })}>
              🔒
            </div>
            <h3 class={css({
              fontSize: '1.25rem',
              fontWeight: 'bold',
              marginBottom: '1rem',
              color: 'gray.900'
            })}>
              型安全
            </h3>
            <p class={css({
              color: 'gray.600',
              lineHeight: '1.6'
            })}>
              TypeScriptによる完全な型安全性。IDEでの自動補完とエラー検出でDXを向上
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Demo Section -->
    <section class={css({
      padding: '4rem 0',
      background: 'gray.50'
    })}>
      <div class={css({
        maxWidth: '1200px',
        margin: '0 auto',
        padding: '0 2rem'
      })}>
        <h2 class={css({
          fontSize: '2.5rem',
          fontWeight: 'bold',
          textAlign: 'center',
          marginBottom: '3rem',
          color: 'gray.900'
        })}>
          コンポーネントサンプル
        </h2>
        
        <!-- Buttons Demo -->
        <div class={css({
          marginBottom: '3rem',
          padding: '2rem',
          background: 'white',
          borderRadius: '12px',
          border: '1px solid',
          borderColor: 'gray.200'
        })}>
          <h3 class={css({
            fontSize: '1.5rem',
            fontWeight: 'bold',
            marginBottom: '1.5rem',
            color: 'gray.900'
          })}>
            ボタン
          </h3>
          <div class={css({
            display: 'flex',
            gap: '1rem',
            flexWrap: 'wrap',
            alignItems: 'center'
          })}>
            <button class={button({ variant: 'primary' })}>Primary</button>
            <button class={button({ variant: 'outline' })}>Outline</button>
            <button class={button({ variant: 'ghost' })}>Ghost</button>
            <button class={button({ variant: 'primary', size: 'sm' })}>Small</button>
            <button class={button({ variant: 'primary', size: 'lg' })}>Large</button>
          </div>
        </div>

        <!-- Colors Demo -->
        <div class={css({
          padding: '2rem',
          background: 'white',
          borderRadius: '12px',
          border: '1px solid',
          borderColor: 'gray.200'
        })}>
          <h3 class={css({
            fontSize: '1.5rem',
            fontWeight: 'bold',
            marginBottom: '1.5rem',
            color: 'gray.900'
          })}>
            カラーパレット
          </h3>
          <div class={css({
            display: 'grid',
            gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))',
            gap: '1rem'
          })}>
            <div data-color-palette="teal" class={css({
              padding: '1rem',
              backgroundColor: 'colorPalette.500',
              color: 'white',
              borderRadius: '8px',
              textAlign: 'center',
              fontWeight: 'medium'
            })}>
              Teal
            </div>
            <div data-color-palette="mint" class={css({
              padding: '1rem',
              backgroundColor: 'colorPalette.400',
              color: 'white',
              borderRadius: '8px',
              textAlign: 'center',
              fontWeight: 'medium'
            })}>
              Mint
            </div>
            <div data-color-palette="sage" class={css({
              padding: '1rem',
              backgroundColor: 'colorPalette.500',
              color: 'white',
              borderRadius: '8px',
              textAlign: 'center',
              fontWeight: 'medium'
            })}>
              Sage
            </div>
            <div data-color-palette="cream" class={css({
              padding: '1rem',
              backgroundColor: 'colorPalette.400',
              color: 'gray.800',
              borderRadius: '8px',
              textAlign: 'center',
              fontWeight: 'medium'
            })}>
              Cream
            </div>
            <div data-color-palette="peach" class={css({
              padding: '1rem',
              backgroundColor: 'colorPalette.400',
              color: 'white',
              borderRadius: '8px',
              textAlign: 'center',
              fontWeight: 'medium'
            })}>
              Peach
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Get Started Section -->
    <section class={css({
      padding: '4rem 0',
      background: 'linear-gradient(135deg, teal.500, mint.400)',
      color: 'white',
      textAlign: 'center'
    })}>
      <div class={css({
        maxWidth: '800px',
        margin: '0 auto',
        padding: '0 2rem'
      })}>
        <h2 class={css({
          fontSize: '2.5rem',
          fontWeight: 'bold',
          marginBottom: '1rem'
        })}>
          始めてみよう
        </h2>
        <p class={css({
          fontSize: '1.2rem',
          marginBottom: '2rem',
          opacity: '0.9'
        })}>
          Anko-UIで美しく機能的なUIを構築しましょう
        </p>
        <div class={css({
          display: 'flex',
          gap: '1rem',
          justifyContent: 'center',
          '@media (max-width: 768px)': {
            flexDirection: 'column',
            alignItems: 'center'
          }
        })}>
          <a href="/anko-ui/docs" class={css({
            display: 'inline-block',
            padding: '0.75rem 2rem',
            background: 'white',
            color: 'teal.600',
            borderRadius: '8px',
            textDecoration: 'none',
            fontWeight: 'medium',
            transition: 'all 0.2s',
            _hover: {
              transform: 'translateY(-2px)',
              boxShadow: '0 4px 12px rgba(0,0,0,0.15)'
            }
          })}>
            ドキュメントを見る
          </a>
          <a href="/anko-ui/playground" class={css({
            display: 'inline-block',
            padding: '0.75rem 2rem',
            border: '2px solid white',
            color: 'white',
            borderRadius: '8px',
            textDecoration: 'none',
            fontWeight: 'medium',
            transition: 'all 0.2s',
            _hover: {
              background: 'white',
              color: 'teal.600'
            }
          })}>
            プレイグラウンドで試す
          </a>
        </div>
      </div>
    </section>
  </div>
</Layout>
